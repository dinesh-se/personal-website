{
	"kind": "task-pipeline",
	"tasksRoot": "/home/dinesh-se/Dev/personal-website/openspec/changes/current/tasks",
	"plan": [
		{
			"id": "T-001",
			"slug": "per-route-loading-and-error-pages",
			"milestone": 1,
			"coversFeatures": [
				"Per-route loading.tsx",
				"Per-route error.tsx (hybrid)"
			],
			"deps": [],
			"userTest": "Run `npm run dev`, visit each route and verify skeleton loaders appear during initial paint. Force an error on one route, verify the error.tsx renders. Run `npm test`, `npm run lint`, `npm run build`, `npm run typecheck`, and `npx playwright test` — all must pass."
		},
		{
			"id": "T-002",
			"slug": "experience-formatdate-bug-fix",
			"milestone": 1,
			"coversFeatures": ["Experience formatDate bug fix"],
			"deps": [],
			"userTest": "Run `npm test` to verify the Experience snapshot still passes. Visit the home page and about page, verify all organization dates display as MMM YYYY (e.g., 'Jan 2024') with no 'Invalid Date' strings. Run `npm run build` and `npm run lint` — zero errors."
		},
		{
			"id": "T-003",
			"slug": "generate-metadata-migration",
			"milestone": 1,
			"coversFeatures": ["generateMetadata migration"],
			"deps": ["T-001"],
			"userTest": "Run `npm run build` — verify zero errors and that all 5 pages produce correct metadata. Verify `robots.txt` and `sitemap.xml` are served correctly at build time. Run `npm test` and `npm run lint` — zero errors."
		},
		{
			"id": "T-004",
			"slug": "partial-prerendering-and-typed-routes",
			"milestone": 2,
			"coversFeatures": ["Partial Prerendering (PPR)", "Typed routes"],
			"deps": ["T-001", "T-002", "T-003"],
			"userTest": "Run `npm run build` — verify PPR is enabled and the build output shows incremental prerendering. Run `npm test`, `npm run lint`, `npm run typecheck` — all pass. Visit each route and verify static shell streams on first paint."
		}
	],
	"planComplete": true,
	"tasks": {
		"T-001": {
			"id": "T-001",
			"slug": "per-route-loading-and-error-pages",
			"path": "/home/dinesh-se/Dev/personal-website/openspec/changes/current/tasks/T-001",
			"milestone": 1,
			"deps": [],
			"fileScope": [
				"src/app/loading.tsx",
				"src/app/error.tsx",
				"src/app/about/loading.tsx",
				"src/app/about/error.tsx",
				"src/app/projects/loading.tsx",
				"src/app/projects/error.tsx",
				"src/app/blog/loading.tsx",
				"src/app/blog/error.tsx",
				"src/app/uses/loading.tsx",
				"src/app/uses/error.tsx"
			],
			"coversFeatures": [
				"Per-route loading.tsx",
				"Per-route error.tsx (hybrid)"
			],
			"userTest": "Run npm run dev, visit each route and verify skeleton loaders appear during initial paint. Force an error on one route, verify the error.tsx renders. Run npm test, npm run lint, npm run build, npm run typecheck, and npx playwright test — all must pass.",
			"acceptanceCriteria": [
				"Root loading.tsx exists and renders skeleton placeholders matching the root layout structure",
				"Root error.tsx exists, catches all unhandled errors, and renders skeleton-based fallback UI",
				"Each of the 5 route directories has its own loading.tsx with skeleton loaders matching the page layout",
				"Each of the 5 route directories has its own error.tsx with error UI consistent with the skeleton loading pattern",
				"All skeleton loaders visually match the corresponding page layout",
				"All error pages display a user-friendly message and a link to navigate back home",
				"npm run build completes with zero TypeScript errors and zero ESLint errors",
				"npm test — all 8 Jest snapshot tests pass",
				"npm run lint — zero ESLint errors",
				"npm run typecheck — zero TypeScript errors",
				"npx playwright test — all 9 E2E tests pass",
				"All 5 pages render correctly in both light and dark modes",
				"Dark mode styles apply correctly on all pages via system preference toggle",
				"The mobile navigation menu opens and closes correctly on a mobile viewport width",
				"The active navigation link is highlighted in both Header and Footer when on any page",
				"All five pages render without runtime errors when visiting each route in a browser",
				"The production build succeeds with zero TypeScript and zero ESLint errors",
				"The CI workflow runs on Node 24 and passes lint and test steps",
				"No visual changes to existing UI components — only new loading/error files added"
			]
		},
		"T-002": {
			"id": "T-002",
			"slug": "experience-formatdate-bug-fix",
			"path": "/home/dinesh-se/Dev/personal-website/openspec/changes/current/tasks/T-002",
			"milestone": 1,
			"deps": [],
			"fileScope": ["src/components/Experience/Experience.tsx"],
			"coversFeatures": ["Experience formatDate bug fix"],
			"userTest": "Run npm test to verify the Experience snapshot still passes. Visit the home page and about page, verify all organization dates display as MMM YYYY with no Invalid Date strings. Run npm run build and npm run lint — zero errors.",
			"acceptanceCriteria": [
				"formatDate() correctly parses DD/MM/YYYY date strings (e.g., 31/01/2024 → Jan 2024)",
				"formatDate() produces correct output for all date formats present in Hygraph data",
				"No Invalid Date strings appear anywhere in the rendered output",
				"The displayed date format remains MMM YYYY — no visual change to the output format",
				"npm test — all 8 Jest snapshot tests pass",
				"npm run build completes with zero TypeScript errors and zero ESLint errors",
				"npm run lint — zero ESLint errors",
				"npm run typecheck — zero TypeScript errors",
				"npx playwright test — all 9 E2E tests pass",
				"All 5 pages render correctly in both light and dark modes",
				"Dark mode styles apply correctly on all pages via system preference toggle",
				"The mobile navigation menu opens and closes correctly on a mobile viewport width",
				"The active navigation link is highlighted in both Header and Footer when on any page",
				"All five pages render without runtime errors when visiting each route in a browser",
				"The production build succeeds with zero TypeScript and zero ESLint errors",
				"The CI workflow runs on Node 24 and passes lint and test steps",
				"No visual changes to existing UI components — only the date parsing logic is fixed"
			]
		},
		"T-003": {
			"id": "T-003",
			"slug": "generate-metadata-migration",
			"path": "/home/dinesh-se/Dev/personal-website/openspec/changes/current/tasks/T-003",
			"milestone": 1,
			"deps": ["T-001"],
			"fileScope": [
				"src/app/layout.tsx",
				"src/app/about/page.tsx",
				"src/app/blog/page.tsx",
				"src/app/projects/page.tsx",
				"src/app/uses/page.tsx",
				"src/app/robots.ts",
				"src/app/sitemap.ts"
			],
			"coversFeatures": ["generateMetadata migration"],
			"userTest": "Run npm run build — verify zero errors and that all 5 pages produce correct metadata. Verify robots.txt and sitemap.xml are served correctly. Run npm test, npm run lint, npm run typecheck — all pass.",
			"acceptanceCriteria": [
				"Root layout.tsx uses generateMetadata instead of static export const metadata",
				"All 5 routes have generateMetadata functions",
				"Each route's metadata includes a page-specific title and description",
				"robots.ts is either removed or converted to use generateMetadata pattern",
				"sitemap.ts is either removed or converted to use generateMetadata pattern",
				"npm run build completes with zero TypeScript errors and zero ESLint errors",
				"npm test — all 8 Jest snapshot tests pass",
				"npm run lint — zero ESLint errors",
				"npm run typecheck — zero TypeScript errors",
				"npx playwright test — all 9 E2E tests pass",
				"All 5 pages render correctly in both light and dark modes",
				"Dark mode styles apply correctly on all pages via system preference toggle",
				"The mobile navigation menu opens and closes correctly on a mobile viewport width",
				"The active navigation link is highlighted in both Header and Footer when on any page",
				"All five pages render without runtime errors when visiting each route in a browser",
				"The production build succeeds with zero TypeScript and zero ESLint errors",
				"The CI workflow runs on Node 24 and passes lint and test steps",
				"No visual changes to existing UI components — only metadata generation is changed"
			]
		},
		"T-004": {
			"id": "T-004",
			"slug": "partial-prerendering-and-typed-routes",
			"path": "/home/dinesh-se/Dev/personal-website/openspec/changes/current/tasks/T-004",
			"milestone": 2,
			"deps": ["T-001", "T-002", "T-003"],
			"fileScope": ["next.config.mjs"],
			"coversFeatures": ["Partial Prerendering (PPR)", "Typed routes"],
			"userTest": "Run npm run build — verify zero errors and that the build output shows PPR is active. Run npm test, npm run lint, npm run typecheck — all pass. Visit each route and verify static shell streams on first paint.",
			"acceptanceCriteria": [
				"next.config.mjs has experimental.ppr: incremental enabled",
				"npm run build completes with zero TypeScript errors and zero ESLint errors",
				"npm run build output shows PPR is active",
				"npm test — all 8 Jest snapshot tests pass",
				"npm run lint — zero ESLint errors",
				"npm run typecheck — zero TypeScript errors",
				"npx playwright test — all 9 E2E tests pass",
				"All 5 pages render correctly in both light and dark modes",
				"Dark mode styles apply correctly on all pages via system preference toggle",
				"The mobile navigation menu opens and closes correctly on a mobile viewport width",
				"The active navigation link is highlighted in both Header and Footer when on any page",
				"All five pages render without runtime errors when visiting each route in a browser",
				"The production build succeeds with zero TypeScript and zero ESLint errors",
				"The CI workflow runs on Node 24 and passes lint and test steps",
				"No visual changes to existing UI components — only PPR configuration is added"
			]
		}
	},
	"review": {
		"checked": true,
		"passed": true,
		"issues": []
	},
	"approved": true,
	"completed": true,
	"readTurn": 2
}
